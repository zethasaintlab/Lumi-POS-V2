A labelled text field: label above, optional hint or error below, placeholder never standing in for the label.

```jsx
<Input label="Saldo awal kas" value={amount} onChange={setAmount} type="number" hint="Diperiksa lagi saat tutup shift." />
<Input label="Alasan pembatalan" error="Alasan pembatalan wajib diisi." />
```
